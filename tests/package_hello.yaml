---

command:
    "go install stringutil && go run src/hello/main.go":
        exit-status: 0
        timeout: 120000
        stdout:
            - /Hello world !/
            - /! dlrow olleH/
            - /Hello world !/